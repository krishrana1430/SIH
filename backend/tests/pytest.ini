[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = .
asyncio_mode = auto

markers =
    integration: mark test as an integration test (slow)
    unit: mark test as a unit test (fast)
    auth: mark test as authentication-related
    rate_limit: mark test as rate-limiting related
